# Server port
server.port=8080

# Bind to all network interfaces (for LAN access)
server.address=0.0.0.0

# Logging
logging.level.ro.chess=INFO
logging.pattern.console=%d{HH:mm:ss} [%thread] %-5level %logger{20} - %msg%n

